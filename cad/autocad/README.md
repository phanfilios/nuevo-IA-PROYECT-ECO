# Optional AutoCAD helper

Load `scripts/ecopark_import.lsp` in AutoCAD, then run `ECOPARKIMPORT` to manually select a DXF and insert it as a block. It does not communicate with EcoPark AI, execute network requests, or alter EcoPark data.
