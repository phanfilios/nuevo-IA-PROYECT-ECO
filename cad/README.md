# CAD handoff

Export a validated design as DXF from the application, then open it in AutoCAD or another compatible CAD tool. The DXF uses metres and separated `ECO_*` layers. This folder intentionally contains no active AutoCAD control integration for the MVP.

The optional AutoLISP helper is a manual import convenience only; review the source and choose the DXF file interactively in AutoCAD.
